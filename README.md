# Bridge API Docs

Bridge.js documentation built with React, TypeScript, Vite, i18next, Highlight.js and React Markdown.

## Local development

```bash
npm install
npm run dev
```

## GitHub Pages

Push to a GitHub repository whose default branch is `main`. Open **Settings → Pages**, set **Source** to **GitHub Actions**, then every push to `main` automatically deploys the site.
